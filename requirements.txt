# Corrective RAG System — dependencies
# Core app
streamlit>=1.38.0

# Vector database (local, no API key)
chromadb>=0.5.5

# Document parsing
pypdf>=4.3.1
python-docx>=1.1.2

# HTTP client used to talk to the local Ollama server
requests>=2.32.3

# Optional corrective web-search fallback (best-effort, no API key required).
# The app runs fine without this package installed; the web-search toggle
# will simply be unavailable.
duckduckgo-search>=6.2.6
